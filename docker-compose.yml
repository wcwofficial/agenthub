version: '3.9'
services:
  agenthub-api:
    image: node:24-alpine
    working_dir: /app
    command: sh -c "npm start"
    ports:
      - "8080:8080"
    volumes:
      - ./:/app
    environment:
      PORT: 8080
